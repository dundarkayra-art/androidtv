# Specify the location of your Android SDK installation.
# Update the path below to match where the SDK is installed on your machine.
# For example:
# sdk.dir=/home/username/Android/Sdk
#
# You can also set ANDROID_HOME or ANDROID_SDK_ROOT environment variables instead.

sdk.dir=/path/to/your/android/sdk
